import type { CSSProperties } from "react";

export const defaultButtonStyleProps: CSSProperties = {
  fontFamily: "inherit",
  borderStyle: "inherit",
  borderColor: "inherit",
  backgroundColor: "inherit",
  padding: 0,
};
